import { Section } from '~styles';
import * as S from './styled';
import { Social } from '~components';

export const Footer = () => (
  <S.Footer>
    <Section>
      <Social />
    </Section>
  </S.Footer>
);
